#include "Application.h"

namespace BGE {

// Application is an abstract base class, so the implementation is minimal
// All methods are pure virtual and implemented by derived classes

} // namespace BGE